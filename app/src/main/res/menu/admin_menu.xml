<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn = "navigation_view">

    <group android:checkableBehavior="single">

        <item android:id="@+id/nav_profile"
            android:icon="@drawable/ic_profile"
            android:title="Profile"/>

        <item android:id="@+id/nav_addBook"
            android:icon="@drawable/ic_add"
            android:title="Add Book"/>

        <item android:id="@+id/nav_borrow"
            android:icon="@drawable/ic_scanner"
            android:title="Borrow Book"/>

        <item android:id="@+id/nav_history"
            android:icon="@drawable/ic_history"
            android:title="Borrow History"/>

        <item android:id="@+id/nav_returnBook"
            android:icon="@drawable/ic_book"
            android:title="Return Book Confirmation"/>

        <item android:id="@+id/nav_chatRoom"
            android:icon="@drawable/ic_chat"
            android:title="Chat Room"/>

        <item android:id="@+id/nav_bookingHistory"
            android:icon="@drawable/ic_booking_history"
            android:title="Discussion Room History"/>

    </group>

    <item android:title="Others">
        <menu>
            <item
                android:id="@+id/nav_logout"
                android:icon="@drawable/ic_logout"
                android:title="Logout" />
        </menu>
    </item>
</menu>